
  <h2><%= @cocktail.name %></h2>

<% @cocktail.doses.each do |dose| %>
  <li><%= dose.ingredient.name%> : <%= dose.description %> <%= link_to 'delete', cocktail_dose_path(@cocktail), method: :delete, data: { confirm: "REALLY !?!" } %></li>
  <% end %>

<%= link_to "back to cocktails' list",  cocktails_path, :class => "btn btn-primary" %>
<br>
<%= link_to "add an ingredient",  new_cocktail_dose_path(@cocktail), :class => "btn btn-success" %>

